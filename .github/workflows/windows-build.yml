name: Windows Build

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag (blank = auto: v<version>-build.<run#>)"
        required: false
        type: string
      prerelease:
        description: "Mark the GitHub Release as a pre-release"
        required: false
        type: boolean
        default: true

permissions:
  contents: write

jobs:
  build:
    name: Build Windows x64 exe
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Resolve and validate release tag
        id: meta
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          INPUT_TAG: ${{ inputs.tag }}
        run: |
          if [ -n "$INPUT_TAG" ]; then
            if ! printf '%s' "$INPUT_TAG" | grep -qE '^[A-Za-z0-9][A-Za-z0-9._/-]*$'; then
              echo "::error::Invalid tag '$INPUT_TAG' — allowed characters: letters, digits, and . _ / -"
              exit 1
            fi
            if ! git check-ref-format "refs/tags/$INPUT_TAG"; then
              echo "::error::Invalid Git tag name '$INPUT_TAG' (no '..', '//', leading or trailing '/' or '.', no '.lock' suffix)."
              exit 1
            fi
            tag="$INPUT_TAG"
          else
            ver="$(node -p "require('./package.json').version")"
            tag="v${ver}-build.${GITHUB_RUN_NUMBER}"
          fi
          if gh release view "$tag" >/dev/null 2>&1; then
            echo "::error::A release for tag '$tag' already exists. Choose a different tag, or delete the existing release and its tag first."
            exit 1
          fi
          if git ls-remote --exit-code origin "refs/tags/$tag" >/dev/null 2>&1; then
            echo "::error::Tag '$tag' already exists on the remote. Delete the tag (and any release) or choose a different tag so the release matches this commit."
            exit 1
          fi
          echo "tag=$tag" >> "$GITHUB_OUTPUT"
          echo "Resolved release tag: $tag"

      - run: npm ci

      - name: Build installer + portable exe
        run: npm run build:win

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: kongyo-spec-windows-x64
          path: dist/*.exe
          if-no-files-found: error

      - name: Publish exe to GitHub Release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ steps.meta.outputs.tag }}
          REF_NAME: ${{ github.ref_name }}
          PRERELEASE: ${{ inputs.prerelease }}
        run: |
          flags=()
          if [ "$PRERELEASE" = "true" ]; then flags+=(--prerelease); fi
          gh release create "$TAG" dist/*.exe \
            --target "$GITHUB_SHA" \
            --title "$TAG" \
            --notes "Manual Windows x64 verification build from \`$REF_NAME\` (\`${GITHUB_SHA:0:7}\`)." \
            "${flags[@]}"
          {
            echo "### Windows build published"
            echo "Release: ${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/releases/tag/$TAG"
          } >> "$GITHUB_STEP_SUMMARY"
